import colors from './colors';
import {fontSizes, scale, spaces} from './matrics';
import mainStyles from './styles';
import fonts from './fonts';

export {colors, mainStyles, fontSizes, scale, spaces, fonts};
